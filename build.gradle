buildscript {
    apply from: rootProject.file('dependencies.gradle')
}

allprojects {
    repositories {
        mavenCentral()
    }
}


task clean(type: Delete) {
    delete rootProject.buildDir
}
